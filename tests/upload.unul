ketik("=== MENGUJI PROTOKOL SETOR (POST) ===")

takdir.soft alamat_post = "https://httpbin.org/post"

^^ Array Data UNUL
takdir.soft data_user = ["Gue", "Arsitek LinuxDNC"]

ketik("Mengirim data ke awan...")
takdir.soft hasil_post = setor(alamat_post, data_user)

ketik("Balasan dari awan (Server memantulkan kembali data kita):")
ketik(hasil_post) 

ketik("=== SELESAI ===")